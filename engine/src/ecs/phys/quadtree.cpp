#include "sybilengine/ecs/phys/quadtree.hpp"

namespace sbl {

/*
  Quadtree::Quadtree(const int level, const int maxLevel, const int maxObjects, const Rect bounds)
  : m_level(level), m_maxLevel(maxLevel), m_maxObjects(maxObjects), m_bounds(bounds) {
  }

  void Quadtree::Clear() {
    colliders.clear();
    for (int i = 0; i < 4; i++) {
      nodes[i]->Clear();
      nodes[i] = nullptr;
    }
  }
  */

}